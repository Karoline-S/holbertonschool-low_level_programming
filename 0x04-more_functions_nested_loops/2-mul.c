#include "main.h"
#include <stdio.h>

/**
 * mul - check if variable is a digit
 * @a: automatic variable int
 * @b: automatic variable int
 * Return: Always 1 if digit, 0 otherwise.
 */
int mul(int a, int b)
{
	int x;

	x = a * b;
	return (x);
}
